server:
  port: 8082
  servlet:
    contextPath: /aniShop
  ssl:
    enabled: false
    key-store-type: PKCS12
    key-store: keystore.p12
    key-store-password: mypassword
spring:
  datasource:
    password: Tigrou007
    url: jdbc:postgresql://localhost:5432/anishopdb
    username: postgres
    driverClassName: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        default_schema: anishopdb
    hibernate:
      ddl-auto: validate
#    show-sql: true  Pour voir les lignes sql que hibernate effectue
